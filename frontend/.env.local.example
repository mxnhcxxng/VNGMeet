# Copy to frontend/.env.local. NEXT_PUBLIC_* được nhúng vào bundle browser, nên
# CHỈ dùng anon key (không bao giờ service_role).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:8000
